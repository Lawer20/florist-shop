# Запуск Локального Сайту - Швидкий Старт

## 1. Запуск Frontend (Сайт)

Відкрийте PowerShell в папці проекту:

```powershell
cd C:\Users\Альона Шаповал\.gemini\antigravity\scratch\florist_shop

# Запуск простого HTTP сервера
python -m http.server 8000
```

**Посилання на сайт**: 
- 🌐 http://localhost:8000/shop.html
- 🌐 http://localhost:8000/index.html

## 2. Запуск Backend (API)

Відкрийте **НОВИЙ** PowerShell в папці backend:

```powershell
cd C:\Users\Альона Шаповал\.gemini\antigravity\scratch\florist_shop\backend

# Створити віртуальне середовище (тільки перший раз)
python -m venv venv

# Активувати віртуальне середовище
.\venv\Scripts\Activate.ps1

# Встановити залежності (тільки перший раз)
pip install -r requirements.txt

# Створити .env файл з вашими ключами
copy .env.example .env
# Відредагуйте .env і додайте ваші Stripe keys

# Запустити сервер
python app.py
```

## 3. Тестування

### Без Backend (традиційні платежі працюють)
Якщо backend не запущений:
- ✅ Zelle, PayPal, Cash працюють
- ❌ Card payment буде прихований

### З Backend (всі платежі працюють)
Коли backend запущений на `http://localhost:5000`:
- ✅ Всі 4 методи оплати доступні
- ✅ Card payment через Stripe

### Тестова картка Stripe
```
Номер: 4242 4242 4242 4242
Дата: будь-яка майбутня (12/25)
CVC: будь-які 3 цифри (123)
ZIP: будь-який (12345)
```

## Посилання

- **Сайт**: http://localhost:8000/shop.html
- **Backend API**: http://localhost:5000/health
- **Stripe Dashboard**: https://dashboard.stripe.com/test/payments

## Примітки

> [!IMPORTANT]
> Backend потребує .env файл з вашими Stripe API keys. Без нього сервер не запуститься.

> [!TIP]
> Тримайте обидва PowerShell вікна відкритими під час тестування - одне для frontend, одне для backend.
